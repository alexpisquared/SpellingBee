<Window x:Class="SpellingBee.zBak"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="zBak" Height="300" Width="300">
 <Grid>
  <Viewbox Grid.Column="1" Grid.Row="7" >
   <Grid Height="248" Width="850">
    <Grid.RowDefinitions>
     <RowDefinition Height="1" />
     <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
     <ColumnDefinition Width="3*" />
     <ColumnDefinition Width="2*" />
    </Grid.ColumnDefinitions>
    <!--<DataGrid Name="dataGrid1"  ItemsSource="{Binding}" Grid.ColumnSpan="2" Grid.RowSpan="1">-->
    <!--<DataGrid.Columns>    <DataGridTextColumn Header="ID"        Width="Auto"     Binding="{Binding ID}"   />    <DataGridTextColumn Header="name"      Width="*"        Binding="{Binding PathFileName}" />   </DataGrid.Columns>-->
    <!--</DataGrid>-->
    <DataGrid Name="dataGrid2"  ItemsSource="{Binding}" Grid.Column="0" Grid.Row="1" />
    <DataGrid Name="dataGrid3"  ItemsSource="{Binding}" Grid.Column="1" Grid.Row="1" />
   </Grid>
  </Viewbox>

 </Grid>
</Window>
